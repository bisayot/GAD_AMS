# GAD-AMS Development & Debugging Logs

This document compiles the complete development history and debugging records for the BSU GAD-AMS project, covering all contributions, updates, and bug fixes from May 2026 to June 2026.

---

## 🛠️ Development Logs

| Date | Developer(s) | Feature ID | Task Description | Status | Time (Hrs) | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **05-11-2026** | Joshua Duligas | N/A | Decoupled the system architecture into a separate frontend interface and backend API, aligning with the recommended Vue 3 and CodeIgniter 4 technical stack. | `Completed` | 1 | Initial boilerplate and directory setup for decoupled architecture; no functional modules implemented yet. |
| **05-11-2026** | Joshua Duligas | F-01 - F-16 | Configure and maintain a decoupled cloud stack leveraging Vercel, Render, Aiven, and Cloudflare R2. Manage the end-to-end continuous deployment lifecycle. | `In Progress` | - | Leverages Vercel for frontend, Render for CI4 backend, Aiven for MySQL, and Cloudflare R2 for PDF storage. |
| **05-18-2026** | Nicole Elgamo, Gondrei Ticol | N/A | Started creating Vue.js basic frontend pages based on the PHP prototype (with no features). | `Completed` | 20 | Built Vue components based on the prototype's layout and logic; fixed routing issues as pages were added. |
| **05-25-2026** | Nicole Elgamo, Gondrei Ticol | F-08 | Database design and alterations; connected frontend and backend. | `Pending` | - | Heavy back-and-forth between DB structure changes and frontend API calls. |
| **05-25-2026** | Gondrei Ticol | F-03 | Built working submission forms for Activity Designs and Accomplishment Reports. | `Pending` | 10 | Forms needed to submit properly and store data correctly in the database. |
| **05-26-2026** | Gondrei Ticol | F-04 | Made Activity Design list, Accomplishment Report list, Archive list, and submitted list functional. | `Completed` | 8 | Lists pull submitted records from the database and display them properly per user role. |
| **05-29-2026** | Gondrei Ticol | F-04 | Created View pages for viewing submitted forms. | `Completed` | 8 | A view page template for viewing activity design and accomplishment report forms. |
| **05-31-2026** | Gondrei Ticol | F-05 | Created review pages for AD and AR; added AD revision page, and designed view/review/revision layouts. | `Completed` | 5 | Review pages are for admin user for approving/revising; revision page lets users update their AD. |
| **06-01-2026** | Gondrei Ticol | F-04 | Made dashboard recent list and calendar work; replaced subtitles with placeholders. | `Completed` | 2 | Dashboard shows recently submitted items; calendar displays current date. |
| **06-02-2026** | Joshua Duligas | F-05, F-06, F-03, F-08, F-16 | Implemented the dynamic document routing engine across all system roles and integrated an inline PDF previewing utility. | `Completed` | 2 | Enables role-based routing and seamless file verification during the review lifecycle. |
| **06-08-2026** | Joshua Duligas | F-01 | Implemented Forgot Password feature with email reset tokens and integrated Cloudflare Turnstile security. | `Completed` | 1 | Secure password recovery flow. |
| **06-12-2026** | Mark Ron De Vera | F-07 | Implemented dynamic backend budget summary aggregation and database computation logic. | `Completed` | 1 | Created BudgetController with `/api/budget/summary` to aggregate GAA/other source totals. |
| **06-13-2026** | Nicole Elgamo, Gondrei Ticol | N/A | Created venue table; replaced venue input to dropdown. | `Completed` | 4 | Created a venue table to store venue lists for the venue dropdown. |
| **06-13-2026** | Mark Ron De Vera | F-14 | Dynamically bound GAD Plan Matrix tables and mapped database sources across all user roles. | `Completed` | 2 | Bound matrix views to `/api/budget/gad-plan`, added Attributed Programs, and implemented currency formatting. |
| **06-15-2026** | Mark Ron De Vera | F-10 | Integrated budget monitoring cards and stats widgets in dashboard content views. | `Completed` | 2.5 | Replaced hardcoded dashboard aggregates with live database metrics in all dashboards. |
| **06-19-2026** | Joshua Duligas | F-17 | Design and implement a lightweight, role-based internal messaging system. | `Completed` | 3 | Allows users (TWGs, GAD Staff, Director) to send and receive messages within the platform. |
| **06-22-2026** | Joshua Duligas, Salem Chocyagan | F-15 | Build user management and office/unit management modules, and implement profile settings interface. | `Completed` | 2 | Dashboard administration utilities and user profile management. |
| **06-24-2026** | Mark Ron De Vera | F-08 | Refactored frontend API calls to use a configured API client and dynamic `API_BASE_URL` instead of localhost. | `Completed` | 3 | Resolved cross-origin resource sharing (CORS) issues across multiple dashboards. |
| **06-25-2026** | Joshua Duligas | F-08 | Implement a two-step document deletion that allows users to send documents to a temporary trash bin. | `In Progress` | - | Allows restoring files or triggering permanent deletion. |
| **06-27-2026** | Joshua Duligas, Salem Chocyagan | F-18 | Develop a dedicated activity logs page for each user role to record and display specific user activities. | `Completed` | 2 | Records and displays user-specific actions on the platform. |
| **06-27-2026** | Mark Ron De Vera | F-07 | Normalized budget tables and implemented the Pending Approved vs. Actual Utilized budget tracking flow. | `Completed` | 8 | AD approval sets "Pending Approved" budget; AR approval validates and updates utilized budget. |
| **06-27-2026** | Mark Ron De Vera | F-14 | Updated database schema to support normalized budget tables and cleaned up old test transactions. | `Completed` | 2 | Updated `gad_ams_system.sql` to align with the new schema. |
| **06-28-2026** | Mark Ron De Vera | F-03 | Synced Staff and TWG SubmitAR/SubmitAD views with date, time, holiday validation, and help tooltips. | `Completed` | 5 | Integrated validation check logic on the frontend to check dates against holidays and validate time ranges. |
| **06-29-2026** | Joshua Duligas | F-11, F-12 | Develop dynamic visual analytics charts for gender-demographic data. | `Completed` | 2 | Visual charts on GAD Staff/Admin dashboards and a non-sensitive GDD on the homepage. |

---

## 🐞 Debugging Logs

| Bug ID | Date | Reporter | Module | Severity | Status | Issue Description | Expected Behavior | Solution |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1** | 06-16-2026 | Mark Ron De Vera | `Frontend` | `Low` | `In Progress` | Proposed Budget Requirements in the Activity Design List in archives does not show after it has been archived. | Should show the proposed budget details. | *Under review.* |
| **2** | 06-24-2026 | Mark Ron De Vera | `Backend/API` | `Medium` | `Resolved` | CORS (Cross-Origin Resource Sharing) blocked API requests from frontend due to hardcoded localhost URLs and missing CORS headers. | Frontend should communicate seamlessly with the backend across different origins/domains without CORS blocks. | Configured CORS middleware in the backend and refactored frontend API calls to use a unified Axios client with a dynamic `API_BASE_URL`. |
| **3** | 06-25-2026 | Mark Ron De Vera | `Environment` | `Medium` | `Resolved` | Codespaces environment defaulted to an older PHP version (PHP 8.1/8.0) causing incompatibilities with newer language features in CodeIgniter 4. | Codespace environment should default to PHP 8.2 as specified in system requirements. | Updated the `.devcontainer/devcontainer.json` configuration to explicitly pull and configure the PHP 8.2 image. |
| **4** | 06-27-2026 | Mark Ron De Vera | `Database` | `Medium` | `Resolved` | Old test transactions and un-normalized budget tables were cluttering the database and causing calculation mismatches. | Database should have normalized tables and a clean state for testing. | Updated the database schema (`gad_ams_system.sql`), normalized the budget tables, and purged old test transactions. |
| **5** | 06-28-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | Activity Designs (AD) and Accomplishment Reports (AR) could be submitted with invalid dates (matching official holidays) and invalid time ranges (end time before start time). | Submission forms should validate dates against holidays and prevent submission of invalid date/time ranges. | Integrated date, time, and holiday validation logic in the Staff and TWG `SubmitAR`/`SubmitAD` views, and added helpful tooltips. |
| **6** | 06-28-2026 | Mark Ron De Vera | `DevContainer` | `Low` | `Resolved` | DevContainer post-create setup failed due to a broken symlink command, preventing automatic container initialization. | DevContainer should build and run post-create setup commands without errors. | Removed the broken symlink command and restructured the `postCreateCommand` in `devcontainer.json`. |
| **7** | 06-28-2026 | munchkiiiiin | `Git/Repo` | `Low` | `Resolved` | Uploaded PDF files were being committed to git, cluttering the repository. | Uploaded PDFs should be ignored by Git while maintaining the empty uploads directory structure. | Added uploaded PDFs to `.gitignore` and added a `.gitkeep` file to preserve the uploads directory. |
| **8** | 06-28-2026 | munchkiiiiin | `Git/Repo` | `Low` | `Resolved` | Committed `node_modules` or vendor dependencies were tracked in git. | Dependencies should not be tracked in the repository. | Untracked dependencies and ensured they are ignored via `.gitignore`. |
| **9** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | Text in the Staff Budget Utilization Monitoring table (office names, codes, values) was invisible due to a hardcoded light gray background (`#cbd5e1`) clashing with the light text colors. | Table text should be fully visible and readable against a dark background, matching the dashboard theme. | Changed the `.table-body` background from `#cbd5e1` to `transparent`, letting the dark container gradient show through. |
| **10** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `High` | `Resolved` | TWG users were automatically logged out and redirected to the login page whenever they clicked "Activity Design" in the "New Submission" page. | Clicking "Activity Design" should load the SubmitADView form for TWG users without logging them out. | Fixed the role validation guard in `SubmitADView.vue` by changing the check from `user.value.role !== 'twg'` to `user.value.role !== 'college'` to match the role stored in localStorage. |
| **11** | 06-29-2026 | Mark Ron De Vera | `Frontend/API` | `High` | `Resolved` | Activity Design submission and revision forms failed to submit due to a parameter naming mismatch between the frontend (which used hyphenated names like `venue-id`, `target-participants`, `proposed-budget`, and `budgetary-requirements`) and the backend (which expected snake_case names like `venue_id`, `target_participants`, `proposed_budget`, and `budget_items`). | Form submissions and revisions should succeed and save records to the database. | Updated the parameter names in `SubmitADView.vue` and `ADRevision.vue` (both TWG and Staff versions) to match the backend's expected snake_case names. |
| **12** | 06-29-2026 | Mark Ron De Vera | `Backend/API` | `High` | `Resolved` | The `get-venues` and `get-control-numbers` endpoints were missing `OPTIONS` preflight routes in the backend router. This caused the browser's CORS preflight requests to fail (404/405), blocking the actual `GET` requests and leaving the venue and control number dropdowns empty in the frontend. | Dropdowns should load data successfully via CORS-compliant GET requests. | Added `$routes->options` preflight routes for both `get-venues` and `get-control-numbers` in `Routes.php`. |
| **13** | 06-29-2026 | Mark Ron De Vera | `Frontend/API` | `High` | `Resolved` | Activity Design submission form failed with a `422 Unprocessable Content` error because the frontend uploaded the PDF attachment under the key `design_file`, while the backend expected and validated it under the key `attachment`. | Form submission should succeed and validate the uploaded file correctly. | Updated `SubmitADView.vue` (both TWG and Staff versions) to append the PDF file under the key `attachment` instead of `design_file`. |
| **14** | 06-29-2026 | Mark Ron De Vera | `Database` | `High` | `Resolved` | Activity Design submission failed with a `500 Internal Server Error` because the active `activity_budget_items` (and `accomplishment_budget_items`) database tables were still using the old flat column structure (e.g., `meals_and_snacks`, `accommodation`, etc.), while the backend controllers were refactored to insert into and query a normalized structure (`category`, `item_name`, `sub_item`, `pax`, `amount`). | The active budget tables should have the normalized column structure to prevent database insertion errors. | Recreated the `activity_budget_items` and `accomplishment_budget_items` tables in the running database and updated the SQL schema file (`gad_submission_system.sql`) to use the normalized column structure. |
| **15** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | TWG and GAD Staff users were unable to revise their Activity Designs because there was no "Revise" or "Edit" button on the AD details page (`ADView.vue`) when the design status was "Revision". | A "Revise Design" button should be displayed in the sidebar if the design status is "Revision", redirecting to the revision page. | Added a conditional "✏️ Revise Design" button and corresponding CSS styles to `ADView.vue` (both TWG and Staff versions) when the design status is "Revision". |
| **16** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | When opening the Activity Design revision page (`ADRevision.vue`), the venue dropdown was empty because the page details and the venues list were fetched concurrently. If the design details loaded first, the selected venue ID could not be matched against the empty venues list. | The venue dropdown should correctly show the selected venue on load. | Updated the `onMounted` hook in `ADRevision.vue` (both TWG and Staff versions) to load the venues list using `await` before loading the design details. |
| **17** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `High` | `Resolved` | Opening the GAD Staff Activity Design revision page (`staff/ADRevision.vue`) resulted in a blank white screen due to a `ReferenceError: computed is not defined`. The `computed` function was used in the setup script but was not imported from `vue`. | The revision page should load and render without JavaScript errors. | Imported `computed` from `vue` in the import statement at the top of `staff/ADRevision.vue`. |
| **18** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | On the Activity Design revision page (`ADRevision.vue`), custom venue details were not loaded because the check `if (!design.value.venue_id)` evaluated to `false` when the ID was the string `'Other'`. Additionally, the Form Type dropdown was empty on load because it used hardcoded string values (like `'employee'`, `'inset'`) while the database stores and returns Form Type IDs (like `1`, `2`, `3`). | Custom venue details and the selected Form Type should be correctly populated on the revision page. | Updated the custom venue check in `fetchDesignDetails` to explicitly handle `'Other'`. Refactored `ADRevision.vue` (both TWG and Staff versions) to dynamically fetch and display the form types from the database. |
| **19** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | On the Activity Design revision page (`ADRevision.vue`), the Form Type dropdown was empty on load due to a type mismatch. The backend returned the `form_type` ID as a number, while the dynamic options had their values as strings. Vue's strict equality check (`===`) failed to match them. | The Form Type dropdown should select and display the correct form type on load. | Converted `design.value.form_type` to a string using `String(design.value.form_type || '')` when populating the form data in `ADRevision.vue` (both TWG and Staff versions). |
| **20** | 06-29-2026 | Mark Ron De Vera | `Database/Model` | `High` | `Resolved` | Activity Design submissions and updates were silently discarding `venue_id`, `classification_id`, `gad_mandate_id`, and `gender_issue_id`, leaving them `NULL` in the database. This was because these fields were missing from the `$allowedFields` array in the `ActivityDesignModel`. | All fields should be saved to the database upon submission and update. | Added `venue_id`, `classification_id`, `gad_mandate_id`, and `gender_issue_id` to the `$allowedFields` array in `ActivityDesignModel.php`. |
| **21** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | On the Activity Design revision page (`ADRevision.vue`), the Form Type dropdown was empty because the `fetchFormTypes` method expected the response to be wrapped in a `success`/`data` object. However, the `/api/get-form-types` endpoint returns a raw JSON array of form types, causing the populating condition to fail. | The Form Type dropdown should load the form types from the raw array response. | Modified `fetchFormTypes` in `ADRevision.vue` (both TWG and Staff versions) to assign `response.data` directly to `formTypes.value`. |
| **22** | 06-29-2026 | Mark Ron De Vera | `Backend/Frontend` | `Medium` | `Resolved` | Activity Design and Accomplishment Report detail, review, and revision views were missing GAD alignment details (Activity Classification, GAD Mandate, Gender Issue) submitted in the creation forms. Also, custom mandates and gender issues were not handled during revisions in the backend. | All submitted GAD details should be displayed in detail, review, and revision views, and editable/storable during revisions. | Updated `show` methods in `ActivityDesignController` and `AccomplishmentReportController` to join and select GAD mandate, gender issue, and classification titles. Updated `updateDesign` in `ActivityDesignController` to handle custom mandates and gender issues. Added GAD Alignment section card to all AD/AR detail and review views, and integrated GAD Alignment inputs in `ADRevision.vue` (both TWG and Staff). |
| **23** | 06-29-2026 | Mark Ron De Vera | `Database/Backend` | `High` | `Resolved` | Backend could not connect to MySQL database at 127.0.0.1, causing 500 Internal Server Errors on login and public analytics endpoints, because the MySQL root user was only configured to connect from localhost. | The backend should be able to connect to the database via 127.0.0.1. | Allowed the root user to connect from 127.0.0.1 with an empty password in MySQL: `CREATE USER 'root'@'127.0.0.1' IDENTIFIED BY ''; GRANT ALL PRIVILEGES ON *.* TO 'root'@'127.0.0.1';` |
| **24** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | Form Type in the Activity Design Review page (and other views) displayed the raw database ID (e.g., 2) instead of the descriptive form type name. | The Form Type should show the descriptive name (e.g., "Employees' Activity Design") instead of the integer ID. | Updated the `formatFormType` helper function in all 16 frontend views to map database IDs 1-4 to their respective descriptive names. |
| **25** | 06-29-2026 | Mark Ron De Vera | `Frontend/Backend` | `High` | `Resolved` | Budget remaining calculations ignored pending approved budgets for offices, and the GAD Director (admin) budget monitoring page displayed a completely different core mandate table instead of the detailed office utilization breakdown. | Both GAD Staff and GAD Director should see the same office budget utilization table with correct remaining budget calculations. | Modified the backend `BudgetController` to return `pending_approved`, updated the frontend remaining budget calculation to subtract `pending_approved`, made the `utilized` column read-only, and copied the updated `BudgetView` to the admin directory with adjusted role checks. |
| **26** | 06-29-2026 | Mark Ron De Vera | `Frontend/Router` | `Medium` | `Resolved` | GAD Director (admin) was unable to access or execute budget allocation adjustments because the `budget-allocation` route was only defined under GAD Staff and blocked non-staff roles. | Both GAD Staff and GAD Director should be able to access the budget allocation view and execute adjustments. | Added the `budget-allocation` route under the admin path in `router/index.js`, and updated the role check in `BudgetAllocationView.vue` to allow both 'gad_staff' and 'admin' roles. |
| **27** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | Budget monitoring stats cards and tables did not follow the required conceptual budget flow naming (using 'Total Available Budget' instead of 'Total Allocated Budget' for the remaining balance card) and lacked a card/column for 'Total Pending Budget'. | The stats cards should display 'Total Available Budget' and 'Total Pending Budget', and the table should include the 'Pending Budget' column and 'Available Budget' column. | Renamed the `Total Allocated Budget` card to `Total Available Budget`, replaced the `Total Remaining Budget` card with `Total Pending Budget`, added the `Pending Budget` column to the table, and renamed the `Remaining Budget` column to `Available Budget` in both `staff/BudgetView.vue` and `admin/BudgetView.vue`. |
| **28** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `High` | `Resolved` | Total Available Budget, Total Utilized, and Total Pending cards displayed zero because they were calculated locally as the sum of unmapped office rows instead of fetching the global GAD plan budget pool metrics. | The stats cards at the top of the page should display the global GAD plan budget pool metrics (Total GAD Budget minus pending/utilized). | Modified the `fetchBudgetData` function in both `staff/BudgetView.vue` and `admin/BudgetView.vue` to fetch the global budget summary statistics from `/api/budget/summary`, and updated the stats card refs to bind directly to these global metrics. |
| **29** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | Accomplishment Report submission form had GAD alignment fields (Activity Classification, GAD Mandate, Gender Issue) marked as readonly, preventing editing when they were auto-filled with N/A (due to legacy NULL values). Also, actual budget input fields lacked a comparison with the proposed budget of the selected Activity Design. | All fields in the AR form should be editable. The form should also display the proposed budget breakdown from the selected Activity Design for comparison next to each actual budget input field. | Removed the 'readonly' attribute and 'input-disabled-ar' class from the GAD alignment fields. Added a fetch call to `/api/activity-design/{id}` when an Activity Design is selected, extracted the proposed budget items, and displayed them as 'Proposed: ₱X.XX' beneath the budget titles in both `twg/SubmitARView.vue` and `staff/SubmitARView.vue`. |
| **30** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | In the Accomplishment Report submission form, the detailed items under 'Others' (such as Coffee) from the approved Activity Design were not auto-populated, requiring manual entry. Also, the upload attachment and attachment preview sections were at the bottom of the page instead of being in the left column matching the SubmitAD layout. | The detailed 'Others' sub-items should be auto-populated with empty actual amounts so users can enter spending or delete them. The attachment upload section should be positioned in the left column. | Updated the watch handler on `control_number` in both `twg/SubmitARView.vue` and `staff/SubmitARView.vue` to filter and map the 'Others' sub-items from the fetched Activity Design into the `othersList`. Moved the `attachment-section-container-ar` HTML block to the bottom of the left column. |
| **31** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Low` | `Resolved` | In the Accomplishment Report submission form, there was no 'Back' button next to the 'Submit Report' button, making it inconvenient for users to navigate back to the previous page. | A styled 'Back' button should be placed next to the 'Submit Report' button, matching the design and layout of other submission forms (like SubmitAD). | Added a 'Back' button in the `form-actions-ar` div that triggers `router.back()`, and copied the `.back-button` CSS styles from `SubmitADView.vue` to both `twg/SubmitARView.vue` and `staff/SubmitARView.vue`. |
| **32** | 06-29-2026 | Mark Ron De Vera | `Frontend/Backend` | `High` | `Resolved` | In the Accomplishment Report submission form, the GAD alignment fields (Classification, GAD Mandate, Gender Issue) and Venue were editable text inputs instead of select dropdowns like in the Activity Design (AD) submission form. | These fields should be select dropdowns in the AR form, allowing users to select options, and any edits to the GAD alignment or venue should be saved to the database (updating the associated archived activity design GAD alignment and storing the venue ID). | Replaced the text inputs with select dropdowns in both `twg/SubmitARView.vue` and `staff/SubmitARView.vue`, fetching options from the backend and watching GAD Mandate to fetch Gender Issues dynamically. Updated `AccomplishmentReportController::submitReport` to save the selected `venue_id` and update the associated `archived_activity_designs` table with the selected `classification_id`, `gad_mandate_id`, and `gender_issue_id`. |
| **33** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | In the Accomplishment Report submission form, the 'Others' breakdown sub-items (like Coffee) did not display their proposed budget amounts, making it difficult for users to compare the actual spending with the proposed amount for each specific sub-item. | The proposed amount for each specific 'Others' sub-item should be displayed next to or under its input field. | Updated the `control_number` watch handler in both `twg/SubmitARView.vue` and `staff/SubmitARView.vue` to map the proposed amount of each 'Others' item into the `othersList`. Added a display element `v-if="o.proposed_amount > 0"` under each sub-item row to display the proposed amount. |
| **34** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `High` | `Resolved` | In the Accomplishment Report submission form, the hidden file input element had the HTML5 'required' attribute. Since the input is hidden, if the user didn't upload any files, the browser blocked form submission with the error 'An invalid form control with name=\'\' is not focusable', preventing submission. | The hidden file input should not have the HTML5 'required' attribute. Validation should be handled via JavaScript during submission, showing a user-friendly SweetAlert warning if no files are uploaded. | Removed the `required` attribute from the file input element in both `twg/SubmitARView.vue` and `staff/SubmitARView.vue`, and added a JavaScript validation check in the `submitReport` function to verify if `uploadedFiles` is empty. |
| **35** | 06-29-2026 | Mark Ron De Vera | `Backend` | `High` | `Resolved` | Submitting an Accomplishment Report resulted in a 500 Internal Server Error because `AccomplishmentReportController` did not import or use CodeIgniter's `ResponseTrait`, causing undefined method errors when calling `respondCreated()` and `fail()`. | The controller should successfully process the report submission and return a JSON success response. | Added `use CodeIgniter\API\ResponseTrait;` and `use ResponseTrait;` to `AccomplishmentReportController.php` to enable all API response helper methods. |
| **36** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | In the Accomplishment Report detail/review view (ARView.vue), there was no 'Revise' button shown when the report status was 'Revision Required', preventing users from navigating to the AR Revision form. | A styled 'Revise Report' button should be shown in the assessment sidebar when the report status is 'Revision Required', redirecting the user to the AR Revision page. | Added a 'Revise Report' button in the `action-buttons` div in both `twg/ARView.vue` and `staff/ARView.vue`, visible when `report.status` is 'revision'. Added the `.btn-revise` CSS styles copied from `ADView.vue`. |
| **37** | 06-29-2026 | Mark Ron De Vera | `Frontend/Backend` | `High` | `Resolved` | In AR Revision mode (ARRevision.vue), multiple details were missing or broken: (a) Professional Fee was not loaded due to a name mismatch ('Professional Fee/Honoria' vs 'Professional Fee/Honoraria'); (b) Evaluation ratings were blank because they were mapped from the report object instead of report.evaluation_results; (c) 'Submitted By' only showed the email; (d) Form Type was blank and not editable; (e) GAD alignment fields and Venue were not editable dropdowns; and (f) Reviewer remarks and assessment date were blank because the backend did not save them when requesting a revision. | All submitted AR details (including Professional Fee, evaluations, submitter's full name, and editable Form Type/Venue/GAD alignment dropdowns) should be correctly reflected in revision mode. The remarks and assessment date from the Director should also be visible in the assessment sidebar. | 1. Updated `AccomplishmentReportController::revisionReport` to save `remarks` and `assessment_date`, and `updateReport` to save GAD alignment and venue ID. 2. Updated `twg/ARRevision.vue` and `staff/ARRevision.vue` to: (a) fix Professional Fee name mapping; (b) map evaluations from `fetchedReport.evaluation_results`; (c) display `submitter_name` along with email; (d) render Form Type, Venue, and GAD Alignment as editable select dropdowns; and (e) append these updated fields to the FormData on resubmission. |
| **38** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | In the Accomplishment Report submission form (SubmitARView.vue), GAD alignment and venue dropdowns were not auto-filled when selecting a control number. This occurred due to: (a) Vue's strict type matching (comparing string IDs from the backend to number IDs in option lists); and (b) a race condition where `gender_issue_id` was set before `fetchGenderIssues` finished loading options. | Selecting a control number (Activity Design) should immediately and correctly auto-fill the Activity Classification, GAD Mandate, Gender Issue, and Venue dropdowns with their saved values. | Casted `classification_id`, `gad_mandate_id`, `gender_issue_id`, and `venue_id` to `Number` in the `control_number` watch handler in both `twg/SubmitARView.vue` and `staff/SubmitARView.vue`. Awaited the `fetchGenderIssues` call before setting `gender_issue_id` to ensure options are fully loaded. |
| **39** | 06-29-2026 | Mark Ron De Vera | `Backend/Frontend` | `High` | `Resolved` | When approving an Activity Design in the Admin panel, the server returned a 500 Internal Server Error if the control number was a duplicate. This happened because: (a) the frontend's `calculateNextControl` calculated next control numbers from unapproved designs instead of querying the `control_number` table (leading to duplicate suggestions like 0601); and (b) the backend `approveDesign` method did not check for duplicate control numbers before attempting to insert, causing a mysqli duplicate key exception. | The admin panel should automatically suggest the next sequential control number by querying the `control_number` table. If a duplicate control number is entered, the backend should return a clear 400 Bad Request message instead of a 500 error. | 1. Added a new endpoint `/api/get-next-control-number` and `ActivityDesignController::getNextControlNumber` to query the `control_number` table and calculate the next sequential control number. 2. Updated `admin/ADReview.vue` to fetch the suggested suffix from this endpoint. 3. Added a check in `ActivityDesignController::approveDesign` to verify if the control number already exists and return a 400 Bad Request with a clear message. |
| **40** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Low` | `Resolved` | In the Accomplishment Report submission form, the dropdown options displayed the control number first followed by the activity title, which was less intuitive for users than selecting directly by the Activity Design title. | The dropdown should allow users to select by the approved Activity Design title, and the dropdown label should be 'Approved Activity Design'. | Updated the dropdown label to 'Approved Activity Design' and changed the option text to display only the `activity_title` in both `twg/SubmitARView.vue` and `staff/SubmitARView.vue`. |
| **41** | 06-29-2026 | Mark Ron De Vera | `Frontend` | `Medium` | `Resolved` | In the Accomplishment Report submission (`SubmitARView.vue`) and revision (`ARRevision.vue`) forms, the GAD alignment dropdowns and venue dropdown did not auto-fill correctly when selecting an approved design. This was caused by Vue's strict type matching: the select option values (e.g. `classification.id`, `venue_id`) were bound as strings (e.g. `"1"`) due to the database driver returning them as strings, while the auto-fill watch handler set them on the form object as numbers (e.g. `1`), leading to a type mismatch. | Selecting an approved design should immediately and correctly auto-fill the Activity Classification, GAD Mandate, Gender Issue, and Venue select dropdowns on the UI. | Casted the `:value` attributes of the select options to `Number` (e.g. `:value="Number(classification.id)"`) in both `SubmitARView.vue` and `ARRevision.vue` (for both `twg` and `staff` portals). This guarantees type consistency and successful binding. |
